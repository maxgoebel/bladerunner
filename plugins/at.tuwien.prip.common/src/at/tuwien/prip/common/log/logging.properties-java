# Leave the list of global handlers empty. We programatically
# set the log4j redirection handler for all created loggers
#
# Better would be to directly add the Java2Log4jHandler here,
# but the implementation of java.logging's LogManager uses
# System.getClassLoader and therefore, when running inside
# Eclipse, it does not see that class.
handlers=

# redirect all messages to logj4 and log4j decide
.level=ALL
